{
  "name": "stampedeshield-root",
  "version": "1.0.0",
  "description": "StampedeShield - Root Workspace Wrapper",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix \"B project\"",
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0",
    "serialport": "^12.0.0"
  }
}
